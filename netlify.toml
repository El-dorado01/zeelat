[build]
  functions = "functions"
  publish = "public"
  command = "composer install && php artisan optimize && npm install && npm run build"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200